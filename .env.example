# LangGraph / LangSmith
LANGSMITH_PROJECT=house-agent

# LLM provider
# Do not commit real keys. Copy this file to .env on the server and fill it there.
DEEPSEEK_API_KEY=replace_me

# Existing MySQL database used by the house-agent project.
DB_DIALECT=mysql+pymysql
DB_HOST=8.130.189.100
DB_PORT=3308
DB_NAME=bighthouse
DB_USER=root
DB_PASSWORD=change_me_before_deploy

POSTGRES_DB=house_agent
POSTGRES_USER=house_agent
POSTGRES_PASSWORD=change_me_before_deploy
